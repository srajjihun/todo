# Google 연동 설정 가이드

이 위젯은 **사용자 본인의 Google Cloud OAuth 클라이언트**를 사용합니다. 무료이며 약 5~10분 걸립니다.
한 번만 설정하면 됩니다.

## 1. 프로젝트 만들기
1. https://console.cloud.google.com 접속 후 Google 계정으로 로그인
2. 상단 프로젝트 선택 ▸ **새 프로젝트** ▸ 이름 예: `my-widget` ▸ **만들기**

## 2. API 사용 설정 (2개)
1. 왼쪽 메뉴 **API 및 서비스 ▸ 라이브러리**
2. `Google Calendar API` 검색 ▸ **사용** 클릭
3. `Google Tasks API` 검색 ▸ **사용** 클릭

## 3. OAuth 동의 화면
1. **API 및 서비스 ▸ OAuth 동의 화면**
2. User Type: **외부(External)** 선택 ▸ 만들기
3. 앱 이름(예: `데스크톱 위젯`), 사용자 지원 이메일, 개발자 이메일만 입력 ▸ 저장
4. 범위(Scopes) 단계는 건너뛰어도 됩니다 (앱이 로그인할 때 직접 요청합니다)
5. **테스트 사용자**에 본인 Gmail 주소 추가 ▸ 저장
6. **(중요·권장)** 요약 화면에서 **앱 게시 ▸ 프로덕션으로 전환**
   - 테스트 상태로 두면 로그인이 **7일마다 만료**되어 매주 다시 로그인해야 합니다.
   - 프로덕션 전환 후 로그인할 때 "확인되지 않은 앱" 경고가 떠도 본인만 쓰는 앱이므로
     **고급 ▸ (앱 이름)(안전하지 않음)으로 이동**을 누르면 됩니다.

## 4. OAuth 클라이언트 ID 만들기
1. **API 및 서비스 ▸ 사용자 인증 정보 ▸ 사용자 인증 정보 만들기 ▸ OAuth 클라이언트 ID**
2. 애플리케이션 유형: **데스크톱 앱** ← 반드시 이 유형이어야 합니다!
3. **만들기** ▸ 표시되는 **클라이언트 ID**와 **클라이언트 보안 비밀번호**를 복사해두세요

## 5. 위젯에 입력
1. 위젯의 ⚙(설정) ▸ **구글 연동**에 클라이언트 ID / 클라이언트 보안 비밀 붙여넣기 ▸ **저장**
2. **Google 계정 로그인** 클릭 ▸ 브라우저가 열리면 계정 선택 ▸ 권한 **허용**
3. "로그인이 완료되었습니다" 화면이 뜨면 창을 닫고 위젯으로 돌아오세요
4. 잠시 후 구글 캘린더 일정과 할 일이 위젯에 표시됩니다 🎉

## 문제 해결
| 증상 | 해결 |
|---|---|
| `redirect_uri_mismatch` 오류 | 클라이언트 유형이 **데스크톱 앱**인지 확인 (웹 앱 유형이면 다시 만들기) |
| `access_denied` / 로그인 거부 | OAuth 동의 화면의 **테스트 사용자**에 본인 계정이 있는지 확인 |
| 일주일마다 로그인 풀림 | 3-6번 단계: 동의 화면을 **프로덕션으로 게시** |
| 리프레시 토큰을 받지 못했다는 오류 | https://myaccount.google.com/connections 에서 이 앱 연결 삭제 후 다시 로그인 |
